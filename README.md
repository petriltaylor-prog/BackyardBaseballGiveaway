# Pablo Sanchez Throwing Challenge

A self-contained event raffle website.

## Run Locally

Open `index.html` in a browser.

## Deploy

Import this repository into Vercel using the **Other** framework preset. No build command, output directory, environment variables, or backend setup is required.

Participant data is stored in the browser on the device running the event table. Every participant receives exactly one raffle entry.
